apiVersion: v1
kind: ConfigMap
metadata:
  name: arera-backend-config
  namespace: default
data:
  NODE_ENV: "production"
  PORT: "8080"
  ALLOWED_ORIGINS: "https://tryarera.com,https://www.tryarera.com"
---
apiVersion: v1
kind: Secret
metadata:
  name: arera-backend-secrets
  namespace: default
type: Opaque
stringData:
  FIREBASE_SERVICE_ACCOUNT: "${FIREBASE_SERVICE_ACCOUNT}"
  GEMINI_API_KEY: "${GEMINI_API_KEY}"
  NVIDIA_API_KEY: "${NVIDIA_API_KEY}"
  TWILIO_ACCOUNT_SID: "${TWILIO_ACCOUNT_SID}"
  TWILIO_AUTH_TOKEN: "${TWILIO_AUTH_TOKEN}"
  SENDGRID_API_KEY: "${SENDGRID_API_KEY}"
  RAZORPAY_KEY_ID: "${RAZORPAY_KEY_ID}"
  RAZORPAY_KEY_SECRET: "${RAZORPAY_KEY_SECRET}"
  RAZORPAY_WEBHOOK_SECRET: "${RAZORPAY_WEBHOOK_SECRET}"
  JWT_SECRET: "${JWT_SECRET}"
  WEBHOOK_SIGNING_SECRET: "${WEBHOOK_SIGNING_SECRET}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arera-backend
  namespace: default
  labels:
    app: arera-backend
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: arera-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: arera-backend
        tier: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      serviceAccountName: arera-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: backend
          image: arera-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: arera-backend-config
            - secretRef:
                name: arera-backend-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: arera-backend-svc
  namespace: default
  labels:
    app: arera-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: arera-backend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arera-backend-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.tryarera.com
        - "*.tryarera.com"
      secretName: arera-tls-cert
  rules:
    - host: api.tryarera.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: arera-backend-svc
                port:
                  number: 8080
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: arera-sa
  namespace: default
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: arera-backend-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: arera-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: arera-backend-pdb
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: arera-backend